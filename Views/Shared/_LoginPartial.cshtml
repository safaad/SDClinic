@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager


@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item nav-link  active">
        <a @*class="nav-link text-light"*@ asp-area="Identity" asp-page="/Account/Manage/Index"><button class="btn btn-primary" data-toggle="tooltip" title="Manage"><i class="fa fa-edit"></i></button></a>
    </li>
    <li class="nav-item nav-link active">
        <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button type="submit" class="btn btn-primary" data-toggle="tooltip" title="Logout"><i class="fa fa-sign-out"></i></button>
        </form>
    </li>

}
else
{
@*<ul class="navbar-nav">
    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li>*@
    <li class="nav-item nav-link active">
        <a class="" asp-area="Identity" asp-page="/Account/Login">  <button class="btn btn-primary" data-toggle="tooltip" title="Login"><i class="fa fa-sign-in"></i></button></a>
    </li>
@*</ul>*@
        }
  